def solve(s):
    solved = ""
    for i in range(len(s)):
        char = s[i]
        if i==0 or s[i-1] == " ":
            solved += char.upper()
        else:
            solved += char
    return solved
    
