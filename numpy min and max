import numpy
N,M = map(int, input().split())
values = []
for n in range(N):
    values.append(list(map(int, input().split())))
values = numpy.array(values)
values = numpy.min(values, axis = 1)
print (numpy.max(values))
